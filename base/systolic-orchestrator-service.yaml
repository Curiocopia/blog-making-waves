apiVersion: v1
kind: Service
metadata:
  name: systolic-orchestrator
  labels:
    app: systolic-orchestrator
spec:
  selector:
    app: systolic-orchestrator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: NodePort